# FishyGuard
FishyGuard is a Python desktop app that detects phishing in emails and messages using a fine-tuned DistilBERT transformer. It features a clean Tkinter GUI, stores prediction history in a local SQLite database, and is packaged as a standalone deployable application for easy use.
